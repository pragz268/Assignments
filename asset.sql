CREATE TABLE asset
(
   id int PRIMARY KEY NOT NULL,
   asset_name varchar(255),
   end_time timestamp,
   severity int,
   start_time timestamp
)
;
CREATE UNIQUE INDEX PRIMARY ON asset(id)
;
CREATE TABLE asset
(
   id int PRIMARY KEY NOT NULL,
   asset_name varchar(255),
   end_time timestamp,
   severity int,
   start_time timestamp
)
;
CREATE UNIQUE INDEX PRIMARY ON asset(id)
;
CREATE TABLE asset
(
   id int PRIMARY KEY NOT NULL,
   asset_name varchar(255),
   end_time timestamp,
   severity int,
   start_time timestamp
)
;
CREATE UNIQUE INDEX PRIMARY ON asset(id)
;
